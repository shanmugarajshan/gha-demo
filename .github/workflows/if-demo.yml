name: If Demo
on:
  push:
    branches:
      - main
      - develop
jobs: 
  demo: 
    runs-on: ubuntu-latest
    steps:
      - name: Run this main step
        if: github.ref_name == 'main'
        run: echo "Running this main step"
      - name: Run this step
        run: echo "Running this step"
